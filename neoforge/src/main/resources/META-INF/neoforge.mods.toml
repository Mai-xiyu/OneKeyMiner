modLoader = "javafml"
loaderVersion = "[1,)"
license = "${license}" # Review your options at https://choosealicense.com/.
issueTrackerURL = "https://github.com/Mai-xiyu/OneKeyMiner/issues"
#clientSideOnly=true #optional

[[mods]] #mandatory
modId = "${mod_id}" #mandatory
version = "${version}" #mandatory
displayName = "${mod_name}" #mandatory
#updateJSONURL="https://change.me.example.invalid/updates.json" #optional (see https://mcforge.readthedocs.io/en/latest/gettingstarted/autoupdate/)
displayURL = "https://github.com/Mai-xiyu/OneKeyMiner"
logoFile = "assets/${mod_id}/icon.png" #optional
credits = "${credits}" #optional
authors = $mod_authors #optional
description = '''${description}'''

[[dependencies.${mod_id}]] #optional
modId = "neoforge" #mandatory
mandatory = true #mandatory
versionRange = "[${neoforge_full_version}]"
ordering = "NONE" # The order that this dependency should load in relation to your mod, required to be either 'BEFORE' or 'AFTER' if the dependency is not mandatory
side = "BOTH" # Side this dependency is applied on - 'BOTH', 'CLIENT' or 'SERVER'

[[dependencies.${mod_id}]]
modId = "minecraft"
mandatory = true
versionRange = "${compatible_forgemc_versions}"
ordering = "NONE"
side = "BOTH"
