plugins {
    id 'unimined-fabric'
}

repositories {
    maven { url "https://maven.shedaniel.me/" }
    maven { url "https://maven.terraformersmc.com/releases/" }
}

dependencies {
    modImplementation "net.fabricmc.fabric-api:fabric-api:${gradle.ext.fabric_api_version}"

    // ModMenu and Cloth Config remain optional compile-time integrations.
    // Re-enable them only after matching 1.21.11 artifacts are configured.

    // compileOnly("com.terraformersmc:modmenu:${gradle.ext.modmenu_version}") {
    //     transitive = false
    // }

    // compileOnly("me.shedaniel.cloth:cloth-config-fabric:${gradle.ext.cloth_config_version}") {
    //     exclude(group: "net.fabricmc.fabric-api")
    // }
}
